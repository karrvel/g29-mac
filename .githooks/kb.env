# Health gate config for the g29-mac vault. `export` is required: the hook sources this
# file and runs the scripts as child processes.
export KB_VOLATILITY="durable,decays-with-code,one-shot"
export KB_SKIP_LINT=0
export KB_PLATFORMS="claude"
