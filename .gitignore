# Build output
bin/
contrib/sdl3-probe/g29probe
contrib/sdl3-probe/ffcheck
*.o
*.dSYM/

# carryover KB tooling — disposable local copy, never publish
_meta/

# PII denylist — machine-specific, never commit
.pii-denylist.local

# Digest output from prefilter.py — large, never commit
_kb-digests/

# kb-eval results — may contain sensitive trap data (matches at any depth)
**/kb-eval/results/

# Obsidian UI state — per-machine, never commit
_knowledge/.obsidian/

# macOS
.DS_Store
**/.DS_Store
